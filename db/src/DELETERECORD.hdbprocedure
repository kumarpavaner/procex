PROCEDURE "DELETERECORD"(ID INTEGER, out result "MY_BOOKSHOP_BOOKS")
    LANGUAGE SQLSCRIPT
    SQL SECURITY INVOKER
    -- DEFAULT SCHEMA <default_schema_name>

AS 
BEGIN


    DELETE FROM "MY_BOOKSHOP_BOOKS" WHERE ID = :ID;
            

    result = SELECT * FROM "MY_BOOKSHOP_BOOKS" ;
END 